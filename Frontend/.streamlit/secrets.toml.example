# ── Streamlit Secrets – Sao chép file này thành secrets.toml và KHÔNG commit ──
# Sao chép: cp secrets.toml.example secrets.toml
# File secrets.toml phải nằm trong .gitignore

[admin]
username = "admin"
password = "1"
